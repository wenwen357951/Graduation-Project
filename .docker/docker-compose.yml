version: "3.3"

services:
  conda:
    build: .
    image: nvidia/cuda
    container_name: maskrcnn
    stdin_open: true
    tty: true
    volumes:
      - type: bind
        source: ../
        target: /Graduation-Project
