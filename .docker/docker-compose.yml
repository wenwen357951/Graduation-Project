version: "3.9"

services:
  conda:
    build: .
    container_name: maskrcnn
    stdin_open: true
    tty: true
    volumes:
      - type: bind
        source: ../
        target: /Graduation-Project
